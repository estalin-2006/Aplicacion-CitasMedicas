<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Citasmedicas">
        
        <activity
            android:name="com.estalin.citasmedicasapp.ui.screen.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.estalin.citasmedicasapp.ui.screen.RegisterActivity" />
        <activity android:name="com.estalin.citasmedicasapp.ui.screen.AdminActivity" />
        <activity android:name="com.estalin.citasmedicasapp.ui.screen.MedicoActivity" />
        <activity android:name="com.estalin.citasmedicasapp.ui.screen.PacienteActivity" />
        <activity android:name="com.estalin.citasmedicasapp.ui.screen.AgendarCitaActivity" />
        <activity android:name="com.estalin.citasmedicasapp.ui.screen.GestionPacientesActivity" />
        <activity android:name="com.estalin.citasmedicasapp.MainActivity" />

    </application>

</manifest>
